|                 |                |
| --------------- | -------------- |
| Module Name     | EGL10          |
| Sub-Module Name | Initialization |
| Target Function | eglTerminate   |
| Safety Level    | DAL A          |
| Rationale       | Not derived    |
| Create Date     | 31.08.2026     |

---

| Requirement ID      | Requirement                                                                                                                                           | Information                                                                                                                                                                                                                                                 | Definitions                                                                                                              | Reference                                                 | Impl.-Dep. | Source      | Verification Method |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- | ---------- | ----------- | ------------------- |
| GS-EGL10-IN-TER-001 | The graphics library shall provide the`eglTerminate` API with the interface defined by EGL 1.0.                                                     | This requirement is verified by compiling and linking a test that calls`eglTerminate`.                                                                                                                                                                    | `EGLBoolean eglTerminate(EGLDisplay dpy);`Input Parameters:`dpy`: Specifies the EGL display connection to terminate. | EGL 1.0 Full Specification, 3.2 Initialization, pp. 10-11 | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-002 | The`eglTerminate` function shall return `EGL_TRUE` when called with a valid initialized `EGLDisplay`.                                           | This requirement is verified by initializing a valid display with`eglInitialize`, calling `eglTerminate`, and checking that the returned value is `EGL_TRUE`.                                                                                         |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-003 | The`eglTerminate` function shall leave a successfully terminated display in an uninitialized EGL state.                                             | This requirement is verified by successfully terminating an initialized display, calling`eglQueryString` on that display before reinitialization, checking that it returns `NULL`, and checking that `eglGetError()` returns `EGL_NOT_INITIALIZED`. |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-004 | The`eglTerminate` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY` when `dpy` does not refer to a valid `EGLDisplay`.       | This requirement is verified by calling`eglTerminate` with an invalid display, checking that the return value is `EGL_FALSE`, and checking that `eglGetError()` returns `EGL_BAD_DISPLAY`.                                                          |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-005 | The`eglTerminate` function shall return `EGL_TRUE` when `dpy` is a valid `EGLDisplay` that has not been initialized.                          | This requirement is verified by obtaining a valid`EGLDisplay` without calling `eglInitialize`, calling `eglTerminate`, and checking that the return value is `EGL_TRUE`.                                                                            |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-006 | The`eglTerminate` function shall return `EGL_TRUE` when `dpy` is a valid `EGLDisplay` that has already been terminated.                       | This requirement is verified by terminating a valid initialized display, calling`eglTerminate` on the same display again, and checking that the second call returns `EGL_TRUE`.                                                                         |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
| GS-EGL10-IN-TER-007 | A valid`EGLDisplay` successfully terminated by `eglTerminate` shall be capable of being initialized again by a subsequent `eglInitialize` call. | This requirement is verified by initializing a valid display, successfully terminating it, calling`eglInitialize` again on the same display, and checking that `eglInitialize` returns `EGL_TRUE`.                                                    |                                                                                                                          | EGL 1.0 Full Specification, 3.2 Initialization, p. 11     | No         | Req-EGL-001 | Test                |
