|  |  |
|---|---|
| Module Name | EGL10 |
| Sub-Module Name | RenderingContexts |
| Target Function | `eglCreateContext` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl. Dep. | Source | Verification Method |
|---|---|---|---|---|---|---|---|
| GS-EGL10-RC-CC-001 | The graphics library shall provide an API function, `eglCreateContext`, that creates an OpenGL ES rendering context. |  | `dpy`: specifies the initialized EGL display connection.<br>`config`: specifies the `EGLConfig` for the context.<br>`share_context`: specifies a context with which to share data, or `EGL_NO_CONTEXT`.<br>`attrib_list`: specifies attribute/value pairs terminated by `EGL_NONE`. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-002 | The `eglCreateContext` function shall initialize a new rendering context to the initial OpenGL ES state and return its handle when the call succeeds. | The returned handle is an `EGLContext`. | Success return: valid `EGLContext` handle. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-003 | The graphics library shall permit a context returned by `eglCreateContext` to render to any compatible `EGLSurface`. | Compatible contexts and surfaces have equal color and ancillary buffer depths and are created for the same `EGLDisplay`. | `EGLSurface`: on-screen or off-screen buffer used as a rendering target. | EGL 1.0 Full Specification, 2.2 Rendering Contexts and Drawing Surfaces, p. 3; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-004 | The `eglCreateContext` function shall share all shareable data, except texture object zero, among the new context, `share_context`, and every context already sharing with `share_context` when `share_context` is not `EGL_NO_CONTEXT`. | An arbitrary number of `EGLContext` objects may share data in this manner. | Shareable data in EGL 1.0 includes texture objects, except texture object name zero. | EGL 1.0 Full Specification, 2.4 Shared State, pp. 5-6; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-005 | The `eglCreateContext` function shall create a context without sharing data with an existing context when `share_context` is `EGL_NO_CONTEXT`. |  | `EGL_NO_CONTEXT`: no rendering context / no shared context. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-006 | The `eglCreateContext` function shall accept `attrib_list` as `NULL` or as an empty list whose first attribute is `EGL_NONE`. | EGL 1.0 defines no core context creation attributes. Platform extensions may define additional attributes. | Empty attribute list: `NULL` or `{ EGL_NONE }`. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-007 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` when context creation fails. | The error cause shall be obtainable through `eglGetError()`. | Failure return: `EGL_NO_CONTEXT`. | EGL 1.0 Full Specification, 3.1 Errors, p. 8; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-008 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_CONTEXT` when `share_context` is neither `EGL_NO_CONTEXT` nor a valid EGL rendering context. |  | `EGL_BAD_CONTEXT`: an `EGLContext` argument does not name a valid `EGLContext`. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-009 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_CONFIG` when `config` does not name a valid `EGLConfig`. |  | `EGL_BAD_CONFIG`: an `EGLConfig` argument does not name a valid `EGLConfig`. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-010 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_MATCH` when the server context state of `share_context` cannot share an address space with the new context. |  | `EGL_BAD_MATCH`: arguments are inconsistent. | EGL 1.0 Full Specification, 2.3 Direct Rendering and Address Spaces, p. 5; 3.6.1 Creating Rendering Contexts, pp. 23-24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-011 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_MATCH` when `share_context` was created on a display different from the display referenced by `dpy`. |  | Display mismatch: the shared context and the new context are associated with different EGL displays. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, pp. 23-24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-012 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_MATCH` when `share_context` and the new context are otherwise incompatible. | Hardware-driver and software-renderer contexts are an example of contexts that may be incompatible. | Incompatible contexts: contexts that cannot legally share server context state. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-013 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_ALLOC` when the server lacks resources to allocate the new context. |  | `EGL_BAD_ALLOC`: EGL failed to allocate resources for the requested operation. | EGL 1.0 Full Specification, 3.1 Errors, p. 8; 3.6.1 Creating Rendering Contexts, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-014 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_ATTRIBUTE` when `attrib_list` contains an unrecognized attribute or attribute value. | In EGL 1.0, no core attributes are recognized for context creation; accepted extension attributes are platform dependent. | `EGL_BAD_ATTRIBUTE`: an unrecognized attribute or attribute value was passed in an attribute list. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-015 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_NOT_INITIALIZED` when EGL is not initialized for `dpy`. | The generic EGL error definitions identify initialization failures separately from invalid display handles. | `EGL_NOT_INITIALIZED`: EGL is not initialized, or could not be initialized, for the display. | EGL 1.0 Full Specification, 3.1 Errors, pp. 8-9; 3.2 Initialization, p. 10 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-016 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_DISPLAY` when `dpy` does not name a valid `EGLDisplay`. |  | `EGL_BAD_DISPLAY`: an `EGLDisplay` argument does not name a valid `EGLDisplay`. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.2 Initialization, p. 10 | No | Req-EGL-001 | Test |
