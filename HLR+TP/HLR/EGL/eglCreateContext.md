|  |  |
|---|---|
| Module Name | EGL10 |
| Sub-Module Name | RenderingContexts |
| Target Function | `eglCreateContext` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl. Dep. | Source | Verification Method |
|---|---|---|---|---|---|---|---|
| GS-EGL10-RC-CC-001 | The graphics library shall provide the `eglCreateContext` API with the interface defined by EGL 1.0. | This requirement is verified by compiling and linking a test that calls `eglCreateContext`. | `EGLContext eglCreateContext(EGLDisplay dpy, EGLConfig config, EGLContext share_context, const EGLint *attrib_list);`<br><br>Input parameters:<br>`dpy`: specifies the initialized EGL display connection.<br>`config`: specifies the `EGLConfig` for the context.<br>`share_context`: specifies a context with which to share data, or `EGL_NO_CONTEXT`.<br>`attrib_list`: specifies attribute/value pairs terminated by `EGL_NONE`.<br><br>Return value:<br>Returns an `EGLContext` handle, or `EGL_NO_CONTEXT` when context creation fails. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-002 | The `eglCreateContext` function shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` when `share_context` is a valid and compatible `EGLContext` whose server context state can be shared with the newly created context. | This requirement is verified by creating a valid context and using it as the `share_context` argument in a second `eglCreateContext` call, then checking that the returned handle is not `EGL_NO_CONTEXT`. | Same as GS-EGL10-RC-CC-001. | EGL 1.0 Full Specification, 2.4 Shared State, pp. 5-6; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-003 | The `eglCreateContext` function shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` when `attrib_list` points to an empty attribute list beginning with `EGL_NONE`. | This requirement is verified by calling `eglCreateContext` with `{ EGL_NONE }` as the context attribute list. | Same as GS-EGL10-RC-CC-001. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-004 | The `eglCreateContext` function shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` when `attrib_list` is `NULL`. | This requirement is verified by calling `eglCreateContext` with `NULL` as the context attribute list and checking the returned context handle directly against `EGL_NO_CONTEXT`. | Same as GS-EGL10-RC-CC-001. | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-005 | The `eglCreateContext` function shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` when called with a valid `EGLConfig` whose `EGL_DEPTH_SIZE` is `0`. | This requirement is verified by selecting an `EGLConfig` with `EGL_DEPTH_SIZE` set to `0`, confirming the selected configuration's `EGL_DEPTH_SIZE` using `eglGetConfigAttrib`, and checking that `eglCreateContext` returns a handle other than `EGL_NO_CONTEXT`. | Same as GS-EGL10-RC-CC-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, pp. 13-14; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-006 | The `eglCreateContext` function shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` when called with a valid `EGLConfig` whose `EGL_DEPTH_SIZE` is at least `16`. | This requirement is verified by selecting an `EGLConfig` with `EGL_DEPTH_SIZE` set to `16`, confirming the selected configuration's `EGL_DEPTH_SIZE` is greater than or equal to `16` using `eglGetConfigAttrib`, and checking that `eglCreateContext` returns a handle other than `EGL_NO_CONTEXT`. | Same as GS-EGL10-RC-CC-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, pp. 13-14; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-007 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_CONTEXT` when `share_context` is neither `EGL_NO_CONTEXT` nor a valid `EGLContext`. | This requirement is verified by calling `eglCreateContext` with an invalid `share_context`, checking that the return value is `EGL_NO_CONTEXT`, and checking that `eglGetError()` returns `EGL_BAD_CONTEXT`. | `EGL_BAD_CONTEXT`: an `EGLContext` argument does not name a valid `EGLContext`. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-008 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_CONFIG` when `config` does not name a valid `EGLConfig`. | This requirement is verified by calling `eglCreateContext` with an invalid `config`, checking that the return value is `EGL_NO_CONTEXT`, and checking that `eglGetError()` returns `EGL_BAD_CONFIG`. | `EGL_BAD_CONFIG`: an `EGLConfig` argument does not name a valid `EGLConfig`. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-009 | The `eglCreateContext` function shall return `EGL_NO_CONTEXT` and generate `EGL_BAD_MATCH` when `share_context` is a valid `EGLContext` that is not compatible with the newly created context. | This requirement is verified only when the test environment can create incompatible contexts; the test calls `eglCreateContext` with the incompatible `share_context`, checks that the return value is `EGL_NO_CONTEXT`, and checks that `eglGetError()` returns `EGL_BAD_MATCH`. | `EGL_BAD_MATCH`: arguments are inconsistent. | EGL 1.0 Full Specification, 2.3 Direct Rendering and Address Spaces, p. 5; 3.6.1 Creating Rendering Contexts, pp. 23-24 | No | Req-EGL-001 | Test |
