| Field | Value |
| --- | --- |
| Module Name | EGL10 |
| Sub-Module Name | RenderingContexts |
| Target Function | `eglCreateContext` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GS-EGL10-RC-CC-001 | The graphics library shall provide API function `eglCreateContext(EGLDisplay dpy, EGLConfig config, EGLContext share_context, const EGLint *attrib_list)`. |  | `EGLContext eglCreateContext(EGLDisplay dpy, EGLConfig config, EGLContext share_context, const EGLint *attrib_list);`<br><br>_Input Parameters:_<br>`dpy`: Specifies the EGL display connection.<br>`config`: Specifies the EGL configuration for the new context. A valid configuration for `dpy` is an `EGLConfig` returned for `dpy` by `eglGetConfigs` or `eglChooseConfig` before `dpy` is terminated.<br>`share_context`: Is `EGL_NO_CONTEXT` or an EGL rendering context from which shareable server state is requested.<br>`attrib_list`: Is `NULL` or an attribute/value list terminated by `EGL_NONE`.<br><br>_Return Value:_<br>Returns an `EGLContext` handle on success; otherwise returns `EGL_NO_CONTEXT`. | EGL 1.0 Full Specification, 3.4.2 Lifetime of Configurations, p. 18; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-002 | When `dpy` is initialized, `config` names a valid `EGLConfig` for `dpy`, `share_context` names a valid EGL rendering context whose server state can be shared with the new context, and sufficient server resources are available, `eglCreateContext` shall return an `EGLContext` handle other than `EGL_NO_CONTEXT` and other than `share_context`. |  |  | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-003 | When `dpy` is initialized, `config` names a valid `EGLConfig` for `dpy`, `share_context` is `EGL_NO_CONTEXT`, `attrib_list` is valid (that is, `NULL` or an attribute/value list terminated by `EGL_NONE`), and sufficient server resources are available, `eglCreateContext` shall return an `EGLContext` handle other than `EGL_NO_CONTEXT`. |  |  | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-004 | If `dpy` is initialized, `config` names a valid `EGLConfig` for `dpy`, `attrib_list` is `NULL`, and `share_context` is neither `EGL_NO_CONTEXT` nor a valid EGL rendering context, `eglCreateContext` shall return `EGL_NO_CONTEXT` and set the calling thread's EGL error state to `EGL_BAD_CONTEXT`. |  |  | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-005 | If `dpy` is initialized, `config` does not name a valid `EGLConfig` for `dpy`, `share_context` is `EGL_NO_CONTEXT`, and `attrib_list` is `NULL`, `eglCreateContext` shall return `EGL_NO_CONTEXT` and set the calling thread's EGL error state to `EGL_BAD_CONFIG`. |  |  | EGL 1.0 Full Specification, 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-006 | If `dpy` does not name a valid `EGLDisplay`, `eglCreateContext` shall return `EGL_NO_CONTEXT` and set the calling thread's EGL error state to `EGL_BAD_DISPLAY`. |  |  | EGL 1.0 Full Specification, 3.1 Errors, p. 9 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-CC-007 | After `eglCreateContext` successfully creates a context with a valid `share_context` other than `EGL_NO_CONTEXT`, texture objects created in `share_context` shall be available to the new context. |  |  | EGL 1.0 Full Specification, 2.4 Shared State, pp. 5-6; 3.6.1 Creating Rendering Contexts, p. 23 | No | Req-EGL-001 | Test |
