|                     |                         |
| ------------------- | ----------------------- |
| **Module Name**     | EGL10                   |
| **Sub-Module Name** | ConfigurationManagement |
| **Target Function** | eglGetConfigAttrib      |
| **Safety Level**    | DAL A                   |
| **Rationale**       | Not derived             |
| **Create Date**     | 30.08.2026              |
---

| Requirement ID       | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| :------------------- | :---------- | :---------- | :---------- | :-------- | :--------- | :----- | :------------------ |
| GS-EGL10-CM-GCA-001 | The graphics library shall provide the `eglGetConfigAttrib` API with the interface defined by EGL 1.0. | This requirement is verified by compiling and linking a test that calls `eglGetConfigAttrib`. | `EGLBoolean eglGetConfigAttrib(EGLDisplay dpy, EGLConfig config, EGLint attribute, EGLint *value);`<br/>_Input Parameters:_<br/>&emsp;`dpy`: Specifies the initialized EGL display connection.<br/>&emsp;`config`: Specifies the EGLConfig to query.<br/>&emsp;`attribute`: Specifies the attribute to query.<br/><br/>_Output Parameters:_<br/>&emsp;`value`: Points to storage for the requested attribute value. | EGL 1.0 Full Specification, 3.4.3 Querying Configuration Attributes, p. 19 | No | Req-EGL-001 | Compile and Link Test |
| GS-EGL10-CM-GCA-002 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_BUFFER_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of bits in the color buffer for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-003 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_RED_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of red component bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-004 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_GREEN_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of green component bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-005 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_BLUE_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of blue component bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-006 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_ALPHA_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of alpha component bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-007 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_CONFIG_CAVEAT`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the configuration caveat value for `config` to `value` and return `EGL_TRUE`. | The value is `EGL_NONE`, `EGL_SLOW_CONFIG`, or `EGL_NON_CONFORMANT_CONFIG`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-008 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_CONFIG_ID`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the unique configuration identifier for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-009 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_DEPTH_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of depth-buffer bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-010 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_LEVEL`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the framebuffer level for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-011 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_MAX_PBUFFER_WIDTH`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the maximum pbuffer width for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-012 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_MAX_PBUFFER_HEIGHT`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the maximum pbuffer height for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-013 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_MAX_PBUFFER_PIXELS`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the maximum pbuffer pixel count for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-014 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_NATIVE_RENDERABLE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write whether native rendering APIs can render to the surface for `config` to `value` and return `EGL_TRUE`. | The value is `EGL_TRUE` or `EGL_FALSE`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-015 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_NATIVE_VISUAL_ID`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the native visual identifier for `config` to `value` and return `EGL_TRUE`. | The value is zero when the config does not support windows or has no associated native visual. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-016 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_NATIVE_VISUAL_TYPE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the native visual type for `config` to `value` and return `EGL_TRUE`. | The value is `EGL_NONE` when the config does not support windows or has no associated native visual type. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-017 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_SAMPLE_BUFFERS`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of multisample buffers for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-018 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_SAMPLES`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of samples per pixel for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-019 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_STENCIL_SIZE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the number of stencil-buffer bits for `config` to `value` and return `EGL_TRUE`. |  | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-020 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_SURFACE_TYPE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the supported surface-type bitmask for `config` to `value` and return `EGL_TRUE`. | EGL 1.0 bits are `EGL_WINDOW_BIT`, `EGL_PIXMAP_BIT`, and `EGL_PBUFFER_BIT`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-021 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_TRANSPARENT_TYPE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the transparency type for `config` to `value` and return `EGL_TRUE`. | The value is `EGL_NONE` or `EGL_TRANSPARENT_RGB`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-022 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_TRANSPARENT_RED_VALUE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the transparent red value for `config` to `value` and return `EGL_TRUE`. | The value is defined when `EGL_TRANSPARENT_TYPE` is `EGL_TRANSPARENT_RGB`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-023 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_TRANSPARENT_GREEN_VALUE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the transparent green value for `config` to `value` and return `EGL_TRUE`. | The value is defined when `EGL_TRANSPARENT_TYPE` is `EGL_TRANSPARENT_RGB`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-024 | When `dpy` names an initialized EGLDisplay, `config` names a valid EGLConfig for `dpy`, `attribute` is `EGL_TRANSPARENT_BLUE_VALUE`, and `value` points to writable EGLint storage, the `eglGetConfigAttrib` function shall write the transparent blue value for `config` to `value` and return `EGL_TRUE`. | The value is defined when `EGL_TRANSPARENT_TYPE` is `EGL_TRANSPARENT_RGB`. | | EGL 1.0 Full Specification, 3.4 Configuration Management, Table 3.1, pp. 13-14; 3.4.3, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-025 | When `attribute` is not a valid EGLConfig attribute, the `eglGetConfigAttrib` function shall return `EGL_FALSE` and generate `EGL_BAD_ATTRIBUTE`. | | | EGL 1.0 Full Specification, 3.4.3 Querying Configuration Attributes, p. 19 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-026 | When `config` does not name a valid EGLConfig for `dpy`, the `eglGetConfigAttrib` function shall return `EGL_FALSE` and generate `EGL_BAD_CONFIG`. | | | EGL 1.0 Full Specification, 3.1 Errors, p. 9 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-027 | When `dpy` names a valid EGLDisplay on which EGL is not initialized, the `eglGetConfigAttrib` function shall return `EGL_FALSE` and generate `EGL_NOT_INITIALIZED`. | | | EGL 1.0 Full Specification, 3.1 Errors, pp. 8-9 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCA-028 | When `dpy` does not name a valid EGLDisplay, the `eglGetConfigAttrib` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY`. | | | EGL 1.0 Full Specification, 3.1 Errors, p. 9 | No | Req-EGL-001 | Test |
