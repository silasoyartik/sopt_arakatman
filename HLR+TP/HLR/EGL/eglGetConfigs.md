| Field | Value |
| --- | --- |
| Module Name | EGL10 |
| Sub-Module Name | ConfigurationManagement |
| Target Function | `eglGetConfigs` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GS-EGL10-CM-GCS-001 | The graphics library shall provide the `eglGetConfigs` API with the interface defined by EGL 1.0. | This requirement is verified by compiling and linking a test that calls `eglGetConfigs`. | `EGLBoolean eglGetConfigs(EGLDisplay dpy, EGLConfig *configs, EGLint config_size, EGLint *num_config);`<br><br>Input parameters:<br>`dpy`: specifies the EGL display connection.<br>`config_size`: specifies the capacity of the `configs` array.<br><br>Output parameters:<br>`configs`: returns EGLConfig handles when not `NULL`.<br>`num_config`: returns the number of configurations reported by the call. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-002 | The `eglGetConfigs` function shall return `EGL_TRUE` when `dpy` is an initialized valid EGL display, `configs` is `NULL`, and `num_config` is not `NULL`. | This requirement is verified by calling `eglGetConfigs(dpy, NULL, 0, &num_config)`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-003 | The `eglGetConfigs` function shall write the total number of available configurations to `num_config` when `dpy` is an initialized valid EGL display and `configs` is `NULL`. | The returned `num_config` value is verified after a successful count-only call. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-004 | The `eglGetConfigs` function shall return `EGL_TRUE` when `dpy` is an initialized valid EGL display, `configs` is not `NULL`, and `num_config` is not `NULL`. | This requirement is verified by calling `eglGetConfigs` with a valid `EGLConfig` output array and a valid `num_config` pointer. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-005 | The `eglGetConfigs` function shall write EGLConfig handles into `configs` when `dpy` is an initialized valid EGL display, `configs` is not `NULL`, and the call succeeds. | Each returned EGLConfig handle is verified by calling `eglGetConfigAttrib` with a valid configuration attribute. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-006 | The `eglGetConfigs` function shall write no more than `config_size` EGLConfig handles into `configs` when `configs` is not `NULL` and the call succeeds. | This requirement is verified by using guard or sentinel values outside the permitted `config_size` range and checking that those values remain unchanged after the call. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-007 | The `eglGetConfigs` function shall write the number of returned EGLConfig handles to `num_config` when `configs` is not `NULL` and the call succeeds. | The returned `num_config` value is verified after a successful fetch call. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-008 | The `eglGetConfigs` function shall return `EGL_TRUE` when `config_size` is smaller than the total number of available configurations and `configs` is not `NULL`. | This requirement is verified by first obtaining the total configuration count, then calling `eglGetConfigs` with a non-NULL output array, a non-NULL `num_config`, and a `config_size` smaller than the total count, and checking that the return value is `EGL_TRUE`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-009 | The `eglGetConfigs` function shall return `EGL_TRUE` when `config_size` is equal to the total number of available configurations and `configs` is not `NULL`. | This requirement is verified with a two-step query: first obtain the total count, then call `eglGetConfigs` using that count as `config_size`, a non-NULL output array, and a non-NULL `num_config`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-010 | The `eglGetConfigs` function shall return the complete EGLConfig list when `config_size` is equal to the total number of available configurations and `configs` is not `NULL`. | This requirement is verified by checking that the returned configuration count is equal to the total count obtained by the count-only query, validating each returned `EGLConfig` handle using `eglGetConfigAttrib`, and verifying that each returned configuration has a unique `EGL_CONFIG_ID`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-011 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY` when `dpy` does not name a valid EGL display connection. | This requirement is verified by calling `eglGetConfigs` with `EGL_NO_DISPLAY`, checking that the return value is `EGL_FALSE`, and checking that `eglGetError()` returns `EGL_BAD_DISPLAY`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-012 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_NOT_INITIALIZED` when `dpy` is a valid `EGLDisplay` on which EGL has not been initialized. | This requirement is verified by obtaining a valid `EGLDisplay` without calling `eglInitialize`, calling `eglGetConfigs`, checking that the return value is `EGL_FALSE`, and checking that `eglGetError()` returns `EGL_NOT_INITIALIZED`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-013 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_BAD_PARAMETER` when `dpy` is an initialized valid `EGLDisplay` and `num_config` is `NULL`. | This requirement is verified by calling `eglGetConfigs(dpy, NULL, 0, NULL)` with an initialized valid `EGLDisplay`, checking that the return value is `EGL_FALSE`, and checking that `eglGetError()` returns `EGL_BAD_PARAMETER`. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
