| Field | Value |
| --- | --- |
| Module Name | EGL10 |
| Sub-Module Name | ConfigurationManagement |
| Target Function | `eglGetConfigs` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GS-EGL10-CM-GCS-001 | The graphics library shall provide the `eglGetConfigs` API to query all EGL framebuffer configurations available for a specified EGL display connection. | This API exposes the unfiltered EGLConfig inventory for the display. Selection based on attributes is handled by `eglChooseConfig` or by inspecting returned configs with `eglGetConfigAttrib`. | `EGLBoolean eglGetConfigs(EGLDisplay dpy, EGLConfig *configs, EGLint config_size, EGLint *num_config);`<br><br>Input parameters:<br>`dpy`: specifies the EGL display connection.<br>`config_size`: specifies the capacity of the `configs` array.<br><br>Output parameters:<br>`configs`: returns EGLConfig handles when not `NULL`.<br>`num_config`: returns the number of configurations reported by the call. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-002 | The `eglGetConfigs` function shall return `EGL_TRUE` and write the total number of available configurations to `num_config` when the call succeeds and `configs` is `NULL`. | This supports the first step of a count-and-fetch sequence. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-003 | The `eglGetConfigs` function shall not return any configuration handles in `configs` when `configs` is `NULL`. | `config_size` is ignored in this mode because no output array is supplied. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-004 | The `eglGetConfigs` function shall return `EGL_TRUE` and write valid `EGLConfig` handles into `configs` when the call succeeds and `configs` is not `NULL`. | The returned handles may be used by EGL functions that require a framebuffer configuration. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-005 | The `eglGetConfigs` function shall write no more than `config_size` `EGLConfig` handles into `configs`. | This applies even when more configurations are available on `dpy`; truncation to array capacity is not an error. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-006 | The `eglGetConfigs` function shall write the number of configuration handles actually returned in `configs` to `num_config` when `configs` is not `NULL` and the call succeeds. | When fewer configurations are available than `config_size`, `num_config` reports the available count. When more are available, `num_config` reports the copied count. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-007 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_BAD_PARAMETER` when `num_config` is `NULL`. | `num_config` is a mandatory output parameter for both count-only and fetch modes. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-008 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY` when `dpy` does not name a valid EGL display connection. | Examples include `EGL_NO_DISPLAY` or an otherwise invalid display handle. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-009 | The `eglGetConfigs` function shall return `EGL_FALSE` and generate `EGL_NOT_INITIALIZED` when the EGL display connection specified by `dpy` has not been initialized. | `eglInitialize` must complete successfully for `dpy` before querying configurations. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-010 | The `eglGetConfigs` function shall not modify `configs` or `num_config` when it returns `EGL_FALSE`. | Failure paths must preserve caller-visible output storage. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-011 | The graphics library shall keep `EGLConfig` handles returned by `eglGetConfigs` valid until the `EGLDisplay` from which they were obtained is terminated. | After `eglTerminate(dpy)`, previously returned config handles for that display shall no longer be used. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.2 Lifetime of Configurations, p. 18 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-012 | The `eglGetConfigs` function shall ignore `config_size` when `configs` is `NULL`. | This allows callers to pass `0` during count-only queries. | Same as GS-EGL10-CM-GCS-001. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
