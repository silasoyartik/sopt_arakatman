| Field | Value |
| --- | --- |
| Module Name | EGL10 |
| Sub-Module Name | ConfigurationManagement |
| Target Function | `eglGetConfigs` |
| Safety Level | DAL A |
| Rationale | Not derived |
| Create Date | 31.08.2026 |

| Requirement ID | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GS-EGL10-CM-GCS-001 | The graphics library shall provide API function `eglGetConfigs(EGLDisplay dpy, EGLConfig *configs, EGLint config_size, EGLint *num_config)`. |  | `EGLBoolean eglGetConfigs(EGLDisplay dpy, EGLConfig *configs, EGLint config_size, EGLint *num_config);`<br><br>_Input Parameters:_<br>`dpy`: Specifies the EGL display connection.<br>`configs`: Is `NULL` for a count-only query or points to a buffer that receives `EGLConfig` handles.<br>`config_size`: Specifies the number of `EGLConfig` elements in the buffer pointed to by `configs`.<br><br>_Output Parameter:_<br>`num_config`: Shall not be `NULL` and points to memory that receives the configuration count defined by the call form. | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-002 | When `dpy` is initialized, `configs` is `NULL`, `config_size` is `0`, and `num_config` is not `NULL`, `eglGetConfigs` shall return `EGL_TRUE` and write the total number of configurations available on `dpy` to the memory area pointed to by `num_config`. |  |  | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-003 | When `dpy` is initialized, `configs` points to a buffer of `config_size` `EGLConfig` elements, `config_size` is greater than `0`, and `num_config` is not `NULL`, `eglGetConfigs` shall return `EGL_TRUE` and write valid `EGLConfig` handles for `dpy` to each element from `configs[0]` through `configs[*num_config - 1]`. |  |  | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15; 3.4.2 Lifetime of Configurations, p. 18 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-004 | On a successful `eglGetConfigs` call with `configs` not `NULL`, `eglGetConfigs` shall not write an `EGLConfig` handle outside the buffer of `config_size` elements and shall write a value no greater than `config_size` to the memory area pointed to by `num_config`. |  |  | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-005 | On a successful `eglGetConfigs` call with `configs` not `NULL`, `eglGetConfigs` shall write the number of returned `EGLConfig` handles to the memory area pointed to by `num_config`. |  |  | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-006 | If `dpy` is `EGL_NO_DISPLAY`, `configs` is `NULL`, `config_size` is `0`, and `num_config` points to valid memory, `eglGetConfigs` shall return `EGL_FALSE` and set the calling thread's EGL error state to `EGL_BAD_DISPLAY`. |  |  | EGL 1.0 Full Specification, 3.1 Errors, p. 9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-007 | If `dpy` names a valid `EGLDisplay` on which EGL is not initialized, `configs` is `NULL`, `config_size` is `0`, and `num_config` points to valid memory, `eglGetConfigs` shall return `EGL_FALSE` and set the calling thread's EGL error state to `EGL_NOT_INITIALIZED`. |  |  | EGL 1.0 Full Specification, 3.1 Errors, pp. 8-9; 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
| GS-EGL10-CM-GCS-008 | If `dpy` is initialized, `configs` is `NULL`, `config_size` is `0`, and `num_config` is `NULL`, `eglGetConfigs` shall return `EGL_FALSE` and set the calling thread's EGL error state to `EGL_BAD_PARAMETER`. |  |  | EGL 1.0 Full Specification, 3.4.1 Querying Configurations, p. 15 | No | Req-EGL-001 | Test |
