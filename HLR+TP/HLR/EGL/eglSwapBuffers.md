|                     |                   |
| ------------------- | ----------------- |
| **Module Name**     | EGL10             |
| **Sub-Module Name** | BufferPosting     |
| **Target Function** | `eglSwapBuffers`    |
| **Safety Level**    | DAL A             |
| **Rationale**       | Not derived       |
| **Create Date**     | 30.08.2026        |
---

| Requirement ID       | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| :------------------- | :---------- | :---------- | :---------- | :-------- | :--------- | :----- | :------------------ |
| GS-EGL10-BP-SB-001 | The graphics library shall provide the `eglSwapBuffers` API with the interface defined by EGL 1.0. | This requirement is verified by compiling and linking a test that calls `eglSwapBuffers`. | `EGLBoolean eglSwapBuffers(EGLDisplay dpy, EGLSurface surface);`<br/>_Input Parameters:_<br/>&emsp;`dpy`: Specifies the initialized EGL display connection.<br/>&emsp;`surface`: Specifies the current EGLSurface whose color buffer is to be posted. | EGL 1.0 Full Specification, 3.8.1 Posting to a Window, p. 27 | No | Req-EGL-001 | Compile and Link Test |
| GS-EGL10-BP-SB-002 | When `dpy` names an initialized EGLDisplay and `surface` names a valid current window surface for `dpy` with a valid native window, the `eglSwapBuffers` function shall copy the color buffer to the native window and return `EGL_TRUE`. | The implementation may realize posting using a platform-specific mechanism. Verification of the posted content requires native-platform observation. | | EGL 1.0 Full Specification, 3.8.1 Posting to a Window, p. 27 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-BP-SB-003 | When `dpy` names an initialized EGLDisplay and `surface` names a valid current pixmap or pbuffer surface for `dpy`, the `eglSwapBuffers` function shall have no posting effect and return `EGL_TRUE`. | The pbuffer and pixmap surface types are exercised as independent subcases. The pbuffer return value is directly testable; observing the pixmap no-posting behavior requires native-platform support. | | EGL 1.0 Full Specification, 3.8.1 Posting to a Window, p. 27; 3.8.4 Posting Errors, p. 29 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-BP-SB-005 | When the native window associated with a current window `surface` was resized and EGL did not already resize the surface transparently, the `eglSwapBuffers` function shall resize `surface` to match the native window before copying pixels. | Native-platform operations are required to resize and observe the window. Newly allocated buffer contents are undefined when the surface grows, and pixels outside the new extent are lost when it shrinks. | | EGL 1.0 Full Specification, 3.8.1 Posting to a Window, pp. 27-28 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-BP-SB-006 | When `dpy` and `surface` are the display and surface of the calling thread's current context, the `eglSwapBuffers` function shall perform an implicit `glFlush`. | Verification requires OpenGL ES commands or implementation instrumentation. | | EGL 1.0 Full Specification, 3.8.3 Posting Semantics, p. 28 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-BP-SB-007 | When `surface` does not name a valid EGLSurface, the `eglSwapBuffers` function shall return `EGL_FALSE` and generate `EGL_BAD_SURFACE`. | | | EGL 1.0 Full Specification, 3.8.4 Posting Errors, p. 29 | No | Req-EGL-001 | Test |
| GS-EGL10-BP-SB-008 | When `surface` is not bound to the calling thread's current context, the `eglSwapBuffers` function shall return `EGL_FALSE` and generate `EGL_BAD_SURFACE`. | | | EGL 1.0 Full Specification, 3.8.4 Posting Errors, p. 29 | No | Req-EGL-001 | Test |
| GS-EGL10-BP-SB-009 | When the native window associated with `surface` is no longer valid and the invalid native handle can be detected, the `eglSwapBuffers` function shall return `EGL_FALSE` and generate `EGL_BAD_NATIVE_WINDOW`. | Verification requires native-platform operations to invalidate the window. | | EGL 1.0 Full Specification, 3.8.4 Posting Errors, p. 29 | Yes | Req-EGL-001 | Integration Test |
| GS-EGL10-BP-SB-010 | When `dpy` names a valid EGLDisplay on which EGL is not initialized, the `eglSwapBuffers` function shall return `EGL_FALSE` and generate `EGL_NOT_INITIALIZED`. | | | EGL 1.0 Full Specification, 3.1 Errors, pp. 8-9 | No | Req-EGL-001 | Test |
| GS-EGL10-BP-SB-011 | When `dpy` does not name a valid EGLDisplay, the `eglSwapBuffers` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY`. | | | EGL 1.0 Full Specification, 3.1 Errors, p. 9 | No | Req-EGL-001 | Test |
