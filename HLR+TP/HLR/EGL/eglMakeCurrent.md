|                     |                   |
| ------------------- | ----------------- |
| **Module Name**     | EGL10             |
| **Sub-Module Name** | RenderingContexts |
| **Target Function** | `eglMakeCurrent`    |
| **Safety Level**    | DAL A             |
| **Rationale**       | Not derived       |
| **Create Date**     | 30.08.2026        |
---

| Requirement ID       | Requirement | Information | Definitions | Reference | Impl.-Dep. | Source | Verification Method |
| :------------------- | :---------- | :---------- | :---------- | :-------- | :--------- | :----- | :------------------ |
| GS-EGL10-RC-MC-001 | The graphics library shall provide the `eglMakeCurrent` API with the interface defined by EGL 1.0. | This requirement is verified by compiling and linking a test that calls `eglMakeCurrent`. | `EGLBoolean eglMakeCurrent(EGLDisplay dpy, EGLSurface draw, EGLSurface read, EGLContext ctx);`<br/>_Input Parameters:_<br/>&emsp;`dpy`: Specifies the initialized EGL display connection.<br/>&emsp;`draw`: Specifies the surface used for OpenGL ES drawing operations.<br/>&emsp;`read`: Specifies the surface used for pixel readback.<br/>&emsp;`ctx`: Specifies the context to bind. | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Compile and Link Test |
| GS-EGL10-RC-MC-002 | When `dpy` names an initialized EGLDisplay, `ctx` names a valid EGLContext, and `draw` and `read` name valid surfaces compatible with `ctx`, the `eglMakeCurrent` function shall bind `ctx`, `draw`, and `read` to the calling thread and return `EGL_TRUE`. | The bindings can be queried with `eglGetCurrentContext` and `eglGetCurrentSurface`. The same EGLSurface may be specified for `draw` and `read`. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, pp. 24-26 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-003 | When the calling thread has a current rendering context and the inputs identify another valid binding, the `eglMakeCurrent` function shall flush the previous context before making `ctx` current. | Verification of the flush requires OpenGL ES commands or implementation instrumentation. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-004 | When `ctx` is `EGL_NO_CONTEXT` and both `draw` and `read` are `EGL_NO_SURFACE`, the `eglMakeCurrent` function shall release the calling thread's current context without assigning a new one and return `EGL_TRUE`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-005 | When `ctx` is `EGL_NO_CONTEXT` and either `draw` or `read` is not `EGL_NO_SURFACE`, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_MATCH`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-006 | When `ctx` is not `EGL_NO_CONTEXT` and either `draw` or `read` is `EGL_NO_SURFACE`, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_MATCH`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-007 | When `draw` or `read` is not compatible with `ctx`, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_MATCH`. | Compatible contexts and surfaces have equal color and ancillary buffer depths and were created for the same EGLDisplay. | | EGL 1.0 Full Specification, 2.2 Rendering Contexts and Drawing Surfaces, p. 3; 3.6.3, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-008 | When `ctx` is current to another thread, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_ACCESS`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-009 | When `draw` or `read` is bound to a context in another thread, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_ACCESS`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-010 | When `ctx` is not `EGL_NO_CONTEXT` and does not name a valid rendering context, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_CONTEXT`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-011 | When `draw` or `read` is not `EGL_NO_SURFACE` and does not name a valid EGLSurface, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_SURFACE`. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 24 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-012 | When a native window underlying `draw` or `read` is no longer valid and the invalid native handle can be detected, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_NATIVE_WINDOW`. | Verification requires native-platform operations to invalidate the window. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, pp. 24-25 | Yes | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-013 | When `draw` and `read` cannot fit into graphics memory simultaneously, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_MATCH`. | Deterministic verification requires controlled resource exhaustion or fault injection. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Fault-Injection Test |
| GS-EGL10-RC-MC-014 | When the calling thread's previous context has unflushed commands and its previous surface is no longer valid, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_CURRENT_SURFACE`. | Verification requires OpenGL ES commands and native-platform operations. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-015 | When the ancillary buffers for `draw` and `read` cannot be allocated, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_ALLOC`. | Deterministic verification requires controlled allocation failure or fault injection. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Fault-Injection Test |
| GS-EGL10-RC-MC-016 | When `draw` is destroyed after being made current, the graphics library shall continue to process rendering commands and update context state. | Frame-buffer state is undefined in this condition and is therefore not a test oracle. Verification requires OpenGL ES operations. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-017 | When `ctx` is made current for the first time, the `eglMakeCurrent` function shall set the viewport and scissor dimensions to the width and height of `draw`. | This is equivalent to `glViewport(0, 0, w, h)` and `glScissor(0, 0, w, h)`. Verification requires OpenGL ES state queries. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-018 | When `ctx` is made current after its first current binding, the `eglMakeCurrent` function shall not modify the viewport or scissor dimensions. | The client is responsible for resetting these dimensions. Verification requires OpenGL ES state queries. | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Integration Test |
| GS-EGL10-RC-MC-019 | The graphics library shall permit no more than one current rendering context for a particular thread. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-020 | When `dpy` names a valid EGLDisplay on which EGL is not initialized, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_NOT_INITIALIZED`. | | | EGL 1.0 Full Specification, 3.1 Errors, pp. 8-9 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-021 | When `dpy` does not name a valid EGLDisplay, the `eglMakeCurrent` function shall return `EGL_FALSE` and generate `EGL_BAD_DISPLAY`. | | | EGL 1.0 Full Specification, 3.1 Errors, p. 9 | No | Req-EGL-001 | Test |
| GS-EGL10-RC-MC-022 | The graphics library shall permit no more than one context to be bound to a particular surface at a time. | | | EGL 1.0 Full Specification, 3.6.3 Binding Contexts and Drawables, p. 25 | No | Req-EGL-001 | Test |
